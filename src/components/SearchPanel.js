import React from 'react';

function SearchPanel (props) {
    return (
        <div className= "searchPanel">
            <input //onChange={} value={} placeholder="Search..." 
            />
            <button
				className="list-btn"
				//onClick={}
			>List
            </button>
        </div>
    );
  }

export default SearchPanel