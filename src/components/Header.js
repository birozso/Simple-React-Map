import React, { Component} from 'react';

function Header() {
    return (
        <header className="header">
            <h1 className= "h1">Neighbourhood map</h1>
        </header>
    )
}

export default Header;
